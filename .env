# 本地环境：生产构建走 /aire/ 子路径，API 用相对路径（同源 nginx 反代，域名/IP/端口入口通用）
VITE_BASE_PATH=/aire/
VITE_API_BASE_URL=
# 后端 CORS：同源部署通常不触发，保留多入口便于直连调试；npm run dev 由 DEV 回退 localhost:3001
CORS_ORIGIN=http://localhost:5173,http://localhost:3000,http://localhost:8082
